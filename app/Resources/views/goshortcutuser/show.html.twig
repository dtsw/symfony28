{% extends 'base.html.twig' %}

{% block body %}
    <h1>Goshortcutuser</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ goShortcutUser.id }}</td>
            </tr>
            <tr>
                <th>Userid</th>
                <td>{{ goShortcutUser.userid }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{% if goShortcutUser.status %}Yes{% else %}No{% endif %}</td>
            </tr>
            <tr>
                <th>Firstname</th>
                <td>{{ goShortcutUser.firstname }}</td>
            </tr>
            <tr>
                <th>Lastname</th>
                <td>{{ goShortcutUser.lastname }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('goshortcutuser_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('goshortcutuser_edit', { 'id': goShortcutUser.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
